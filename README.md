# Compiling
`Make build`

Should work if you have `ncurses` installed.

# Usage
`./cclock -d [minutes]`

Start countdown.

`./cclock -c` or just `./cclock`

Start in clock mode.

`./clock -u`

Start a stopwatch.

# TODO
- [ ] Display the final time on stopwatch.
